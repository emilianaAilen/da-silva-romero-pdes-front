import { Box, styled } from "@mui/material";

export const Container = styled(Box)(() => ({
  marginTop: '30px',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  width: '100%'
}));